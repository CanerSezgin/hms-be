export enum Gender {
  male = 'm',
  female = 'f',
}

export enum UserType {
  admin,
  patient,
  doctor,
  labSpec,
}

export enum TestStatus {
  pending = 'pending',
  done = 'done',
}

export enum TestTypeEnum {
  analysis = 'analysis',
  imaging = 'imaging',
}